title = "State variables that could be declared immutable"
severity = "major"
category = "performance"
weight = 60
description = """
State variables that are not updated following deployment should be declared immutable to save gas.

<!--more-->

## Exploit Scenario


## Recommendation
Add the `immutable` attribute to state variables that never change or are set only in the constructor.

## Learn more
[immutable-states](https://github.com/crytic/slither/wiki/Detector-Documentation#state-variables-that-could-be-declared-immutable) on Slither's wiki.
"""
